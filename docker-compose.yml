services:
  ui:
    build:
      context: packages/ui
      dockerfile: Dockerfile
    ports:
      - "3030:3030"
    environment:
      - ANTHROPIC_API_KEY
    volumes:
      - .:/workspace
      - pi-agent-home:/root/.pi/agent

volumes:
  pi-agent-home:
