services:
  ui:
    build:
      context: .
      dockerfile: packages/ui/Dockerfile
    ports:
      - "3030:3030"
    environment:
      - UI_TOKEN
    volumes:
      - .:/workspace
      - pi-agent-home:/root/.pi/agent

volumes:
  pi-agent-home:
