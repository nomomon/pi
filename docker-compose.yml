services:
  ui:
    build:
      context: .
      dockerfile: packages/ui/Dockerfile
    ports:
      - "3030:3030"
    environment:
      - UI_TOKEN
    volumes:
      - pi-workspace:/workspace
      - pi-agent-home:/home/pi/.pi

volumes:
  pi-workspace:
  pi-agent-home:
